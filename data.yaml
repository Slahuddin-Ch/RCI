# data.yaml
path: /home/user/Desktop/rci/Road_mask/processed_data
train: train/images
val: valid/images
test: test/images

nc: 2  # Number of classes
names: ['road','background']  # Class names (update if you have multiple classes)